// 함수 선언식

function add(a, b) {
  return a + b;
}

console.log(add(2, 3));

// 함수 표현식
const multiply = function (a, b) {
  return a * b;
};

// 화살표 함수
const add = (a, b) => {
  return a + b;
};

// 1급 객체란?? : 다른 객체들에 일반적으로 적용 가능한 연산을
//                모두 지원하는 객체를 가리킨다.
